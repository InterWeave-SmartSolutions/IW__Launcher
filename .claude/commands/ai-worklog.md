---
description: Append a new entry to AI_WORKLOG.md (mandatory for any changes)
---
# /ai-worklog
You MUST run this after making changes in `IW_Launcher/`.

Steps:
1. Identify what changed and why.
2. Append (do not edit old entries) a new section to `AI_WORKLOG.md` using the standard template.
3. Do NOT include secrets (no DB passwords/tokens/private URLs).

If you are unsure of the exact commands run, include a high-level list (e.g. `git status`, `mvn test`).
