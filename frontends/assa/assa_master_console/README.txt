ASSA Master Console (Static HTML Prototype)

Run locally:
- Serve the folder with a static server (recommended):
  python -m http.server 8080
  Then open: http://localhost:8080/index.html

Notes:
- This is a static prototype to illustrate the Master Console IA and UI.
- All /api/* endpoints are placeholders for future wiring.